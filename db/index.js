const Users = require('./user');
const Games = require('./game');
const Chat = require('./messages');
module.exports =  { Users, Games, Chat };

// modules that are exported and then used in other files 